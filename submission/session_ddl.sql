CREATE OR REPLACE TABLE deeptianievarghese22866.session_job_example (
  user_id VARCHAR,
  ip VARCHAR,
  session_id VARCHAR,
  session_start TIMESTAMP,
  session_end TIMESTAMP,
  event_count BIGINT,
  session_start_date TIMESTAMP,
  country VARCHAR,
  state VARCHAR,
  city VARCHAR,
  os VARCHAR,
  browser VARCHAR,
  logged_in_user_status VARCHAR
)
